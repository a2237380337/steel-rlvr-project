# Pipeline-only configuration; never quote its metrics in the resume.
model_name_or_path: Qwen/Qwen3.5-0.8B-Base
model_revision: a9a407bcae463285164cc9133995c515379cebe5
mixed_precision: bf16
local_files_only: true

train_file: data/processed/train.jsonl
validation_file: data/processed/validation.jsonl
data_report_file: data/processed/data_report.json
max_samples: 128
validation_max_samples: 32
output_dir: artifacts/checkpoints/sft-smoke

max_length: 512
max_steps: 20
gradient_accumulation_steps: 4
learning_rate: 2.0e-5
warmup_ratio: 0.05
lr_scheduler_type: cosine
weight_decay: 0.01
max_grad_norm: 1.0
logging_steps: 1
eval_steps: 10
save_steps: 10
save_total_limit: 1

lora_r: 8
lora_alpha: 16
lora_dropout: 0.05
seed: 42
